# ParcelDesk frontend

React + Vite frontend for the order operations workspace. See the [root README](../README.md) for Docker startup, configuration, source-data rules and testing.

```sh
npm ci
npm run dev
```

Run the backend on port 3001; Vite proxies `/api` to it. Shared components live in the existing `components/` directory outside `src/`.
